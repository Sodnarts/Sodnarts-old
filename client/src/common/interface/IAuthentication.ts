/**
 *
 * @export
 * @interface IAuthenticationProps
 */
export interface IAuthenticationProps {
    auth: any;
    theme: {
        primary: string;
        secondary: string;
        border: string;
        background: string;
        divider: string;
        secondaryText: string;
        text: string;
    };
}
